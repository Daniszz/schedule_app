const ReviewSubmit = ({ scheduleData, jobs, resources, onSubmit }) => {
  return (
    <div>
      <h2>Review & Submit</h2>

      {/* Display schedule summary */}
      {/* Display jobs list */}
      {/* Display resources list */}

      <button onClick={onSubmit}>Submit</button>
    </div>
  );
};

export default ReviewSubmit;
